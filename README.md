## Usage:

```shell
vagrant plugin install vagrant-berkshelf --plugin-version '>= 2.0.1'
vagrant plugin install vagrant-omnibus
git clone git@github.com:Nobru/php-vagrant-env.git
cd php-vagrant-env
vagrant up
```
